version: "3.9"

services:
  api:
    build: .
    image: gpt-cell-annotator-api:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GPT_CELL_ANNOTATOR_DATA_DIR=/data
    volumes:
      - ./data/processed:/data
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
