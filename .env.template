# GPT Cell Annotator environment configuration template
# Copy to .env and fill in the values before running locally or deploying.

# OpenAI API key used for live LLM annotations. Leave blank to run in mock mode.
OPENAI_API_KEY=

# Override backend URL for the Streamlit UI (defaults to http://127.0.0.1:8000)
GPT_CELL_ANNOTATOR_API_URL=http://127.0.0.1:8000

# Redis cache connection string (e.g., redis://localhost:6379/0). Leave empty to disable caching.
REDIS_URL=

# Environment name (development, staging, production)
ENVIRONMENT=development
